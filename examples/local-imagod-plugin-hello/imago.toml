name = "local-imagod-plugin-hello-app"
main = "target/wasm32-wasip2/release/local-imagod-plugin-hello-app.wasm"
type = "cli"

[build]
command = "CARGO_TARGET_DIR=target cargo build --target wasm32-wasip2 --release"

[[dependencies]]
version = "0.1.0"
kind = "wasm"
wit = "sizumita:ferris"

[capabilities]
privileged = false
wasi = true

[capabilities.deps]
"sizumita:ferris" = ["sizumita:ferris/says@0.1.0.say"]

[target.default]
remote = "ssh://localhost?socket=/tmp/imagod-local-plugin-hello.sock"
