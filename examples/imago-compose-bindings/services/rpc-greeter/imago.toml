name = "rpc-greeter"
main = "target/wasm32-wasip2/release/rpc_greeter.wasm"
type = "rpc"

[build]
command = "cargo build --target wasm32-wasip2 --release"

[capabilities]
privileged = false
wasi = true
